<!DOCTYPE html>
<html>
<head>
  <title>Admin - ActifQuest</title>
  <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
  <%= csrf_meta_tags %>
</head>

<body>
  <div class="admin-header">
    <div class="container">
      <h1>Actif Quest <small>for Admin</small></h1>
    </div>
  </div>
  <div class="container">
    <% if admin_signed_in? %> 
    <div class="row">
      <div class="col-md-3">
        <div class="list-group">
          <%= admin_menu_item "展示", admin_exhibitions_path %>
          <%= admin_menu_item "グループ", admin_groups_path %>
          <%= admin_menu_item "管理者", admin_admins_path %>
          <%= admin_menu_item "ユーザ", admin_users_path %>
          <%= admin_menu_item "お知らせ", admin_notifications_path %>
        </div>
      </div>
      <div class="col-md-9">
        <% if admin_signed_in? %>
          Logged in as <strong><%= current_admin.email %></strong>.
          <%= link_to 'Edit profile', edit_admin_registration_path, :class => 'navbar-link' %> |
          <%= link_to "Logout", destroy_admin_session_path, method: :delete, :class => 'navbar-link' %>
        <% end %>
        <%= yield %>
      </div>
    </div>
    <% else %>
      <%= yield %>
    <% end %>
  </div>
</body>
</html>
