<!DOCTYPE html>
<html>
<head>
  <title>Admin - ActifQuest</title>
  <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
  <%= csrf_meta_tags %>
</head>

<body>
  <div class="admin-header">
    <div class="container">
      <h1>Actif Quest <small>for Admin</small></h1>
    </div>
  </div>
  <div class="container">
    <div class="row">
      <div class="col-md-3">
      <% if admin_signed_in? %> 
        <div class="list-group">
          <a href="<%= admin_exhibitions_path %>" class="list-group-item">展示一覧 <small>- Exhibitions</small></a>
          <a href="<%= admin_groups_path %>" class="list-group-item">グループ(部活)一覧 <small>- Groups</small></a>
        </div>
      <% end %>
      </div>
      <div class="col-md-9">
        <% if admin_signed_in? %>
          Logged in as <strong><%= current_admin.email %></strong>.
          <%= link_to 'Edit profile', edit_admin_registration_path, :class => 'navbar-link' %> |
          <%= link_to "Logout", destroy_admin_session_path, method: :delete, :class => 'navbar-link'  %>
        <% else %>
          <%= link_to "Sign up", new_admin_registration_path, :class => 'navbar-link'  %> |
          <%= link_to "Login", new_admin_session_path, :class => 'navbar-link'  %>
        <% end %>
        <%= yield %>
      </div>
    </div>
  </div>

</body>
</html>
