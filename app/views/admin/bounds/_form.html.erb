<%= form_for([:admin, @bound], html: {class: "form-horizontal"}) do |f| %>
<% if @bound.errors.any? %>
<div id="error_explanation">
  <h2><%= pluralize(@bound.errors.count, "error") %> prohibited this admin_bound from being saved:</h2>

  <ul>
    <% @bound.errors.full_messages.each do |message| %>
    <li><%= message %></li>
    <% end %>
  </ul>
</div>
<% end %>

<div class="form-group">
  <%= f.label :name, class: "col-sm-3 control-label" %>
  <div class="col-sm-9">
    <%= f.text_field :name, autofocus: true, placeholder: "Name", class: "form-control" %>
    <br>
  </div>
</div>
<br>
<div class="actions">
  <%= admin_submit_btn %>
</div>
<br>
<% end %>
