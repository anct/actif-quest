<p id="notice"><%= notice %></p>

<p class="lead">
  <span class="pink">Name:</span>
  <%= @bound.name %>
  <%= render @beacons %>
</p>

<%= link_to 'Edit', edit_admin_bound_path(@bound) %> |
<%= link_to 'Back', admin_bounds_path %>