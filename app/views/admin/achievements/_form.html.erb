<%= form_for([:admin, @achievement]) do |f| %>
  <% if @achievement.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@achievement.errors.count, "error") %> prohibited this admin_achievement from being saved:</h2>

      <ul>
      <% @achievement.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <span><%= f.label :name %></span><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <span><%= f.label :condition %></span><br>
    <%= f.text_field :condition %>
  </div>
  <div class="field">
    <span><%= f.label :description %></span><br>
    <%= f.text_field :description %>
  </div>
  <div class="field">
    <span><%= f.label :icon_url %></span><br>
    <%= f.file_field :icon_url %>
  </div>
  <br>
  <div class="actions">
    <%= f.submit :class => "btn btn-primary" %>
  </div>
  <br>
<% end %>
